const express = require('express');
const router = express.Router();
// const Your-lower-model-name = require('./details');

// Here You can add own endpoints, for example:
// router.get('/', Your-lower-model-name.get);

module.exports = router;
